import { RecoverP1Form } from "@src/components/features/users";
import { LayoutForm } from "@components/Layouts";

export const RecoverP1 = () => {
  return (
    <LayoutForm title="Recuperar contraseña">
      <RecoverP1Form />
    </LayoutForm>
  );
};
